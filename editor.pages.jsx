import { useContext } from "react"

const Editor = () => {
    let { } = useContext(userContext)
    return(
        <h1>this is coming form the editior page</h1>
    )
}

export default Editor